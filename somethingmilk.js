// for tyson xdddddd

// all of the variables
var milk = 10;
var mPerClick = 1;

// mpc = milk per click
// multiplier is self-explainable right?
function click(mpc, multiplier, debug) {
    let sum = mpc * multiplier;
    milk + sum;
    // "true" can only be set via console
    if (debug == true) {
        console.log("Milk value is now:" + milk);
    }
}